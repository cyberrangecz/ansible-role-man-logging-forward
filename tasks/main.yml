---
# tasks file for rsysloging-server

- name: Install rsyslog-relp
  apt:
    name: rsyslog-relp

- name: "command-monitoring-collector.conf"
  template:
    src: command-monitoring-collector.conf.j2
    dest: /etc/rsyslog.d/command-monitoring-collector.conf
  become: yes

- name: "Copy client-cert for rsyslog"
  copy:
    src: client-cert.pem
    dest: /etc/rsyslog.d/client-cert.pem
  become: yes

- name: "Copy client-key for rsyslog"
  copy:
    src: client-key.pem
    dest: /etc/rsyslog.d/client-key.pem
  become: yes

- name: "Restart rsyslog service"
  service:
    name: rsyslog
    state: restarted
    enabled: yes

- name: Open port on man
  command: iptables -A INPUT -p tcp --dport 514 --jump ACCEPT
  become: yes

- name: Save iptables
  command: iptables-save
  become: yes